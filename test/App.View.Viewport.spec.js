describe('Viewport', function(){
    //loadFixtures('../../index.html');
    
    var scope = "";
    //$httpBackend.when('GET', 'Users/users.json').respond([{id: 1, name: 'Bob'}, {id:2, name: 'Jane'}]);
    //create an empty scope
    
    
    //});
    // tests start here
    it('Title should equal to "CPU Scheduling Simulator 2.0"', function(){
        //console.log(jasmine);
        //expect($("body").text()).toMatch('CPU Scheduling Simulator 2.0');
    });
    

    it('generateProcess function should generate a given quantities of process object', function(){
        //var length = App.Global.processes.legth;
        //var howMany = App.Global.howManyProcesses;
        //viewport.generateProcess(null);
    });
});